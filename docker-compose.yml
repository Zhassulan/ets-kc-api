version: "3.7"
services:
  api-v1-ets:
    restart: always
    image:  zhasst/api-v1-ets:latest
    network_mode: bridge
    volumes:
      - "/var/log/app:/var/log/app"
    ports:
      - "8082:8080"
    container_name: api-v1-ets
    environment:
      - SPRING_PROFILES_ACTIVE=prod
